PREFIX tg: <https://example.org/tourguide#>

SELECT ?cat (COUNT(DISTINCT ?place) AS ?count)
WHERE {
  ?place a tg:Place ;
         tg:rawSubCategory ?cat .
}
GROUP BY ?cat
ORDER BY DESC(?count)
LIMIT 25
